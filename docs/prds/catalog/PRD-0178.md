# PRD-0178 — Default run with no flags executes a full local match

- **PRD ID**: PRD-0178
- **Title**: Default run with no flags executes a full local match
- **Category**: CLI Behavior
- **Requirement source**: docs/prds/PRD-004-controller-and-orchestration.md + SG-C03
- **Implementation chunk**: 8
- **Status**: Not Started

## Requirement statement

The CLI MUST: Default run with no flags executes a full local match.

## Rationale

Required for gradable CLI evidence (HW-F26) and SG-C03's SDK-only CLI rule.

## Acceptance criteria

A CLI smoke test or unit test verifies: Default run with no flags executes a full local match.

## Test / validation method

CLI smoke test in tests/integration/
