# PRD-0221 — Strategy selection (heuristic vs q_learning) is logged once at match start

- **PRD ID**: PRD-0221
- **Title**: Strategy selection (heuristic vs q_learning) is logged once at match start
- **Category**: Logging Requirement
- **Requirement source**: docs/03_architecture.md + SG-C09
- **Implementation chunk**: varies
- **Status**: Not Started

## Requirement statement

The system MUST ensure: Strategy selection (heuristic vs q_learning) is logged once at match start.

## Rationale

SG-T06 requires reportable run evidence; SG-C09 forbids secrets in logs; HW-F26 wants CLI evidence.

## Acceptance criteria

A unit/integration test using a log-capture fixture asserts: Strategy selection (heuristic vs q_learning) is logged once at match start.

## Test / validation method

Unit test with a log-capture fixture (e.g. pytest caplog)
