# PRD-0088 — `.env` used for secrets, `.env-example` committed with placeholders, `.gitignore` excludes `.env`/`*.key`/`*.p

- **PRD ID**: PRD-0088
- **Title**: `.env` used for secrets, `.env-example` committed with placeholders, `.gitignore` excludes `.env`/`*.key`/`*.p
- **Category**: Submission Checklist Item
- **Requirement source**: docs/06_submission_checklist.md
- **Implementation chunk**: 10/11
- **Status**: Not Started

## Requirement statement

The submission MUST satisfy: `.env` used for secrets, `.env-example` committed with placeholders, `.gitignore` excludes `.env`/`*.key`/`*.pem`/`credentials.json`.

## Rationale

Required by the submission checklist merged from both governing PDFs.

## Acceptance criteria

The checklist box is checked and is independently verifiable by review or script.

## Test / validation method

Manual review or automated script, per checklist section
