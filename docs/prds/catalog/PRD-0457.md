# PRD-0457 — Chunk 3 (MCP server/server layer) — Expected output

- **PRD ID**: PRD-0457
- **Title**: Chunk 3 (MCP server/server layer) — Expected output
- **Category**: Chunk Specification Item
- **Requirement source**: docs/04_implementation_chunks.md
- **Implementation chunk**: 3
- **Status**: Not Started

## Requirement statement

Chunk 3's Expected output MUST be: `uv run python -m hw6_race.services.mcp.server_a` (and `_b`) start standalone; a test client can call their tools successfully with a valid token, and is rejected without one.

## Rationale

Each chunk's spec attribute is tracked individually so partial chunk progress stays auditable.

## Acceptance criteria

The implementation/process for Chunk 3 matches its documented Expected output.

## Test / validation method

Manual review against docs/04_implementation_chunks.md at chunk completion
