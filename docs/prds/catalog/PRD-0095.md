# PRD-0095 — All dependency/build/test/run commands go through `uv` (`uv sync`, `uv add`, `uv run python`, `uv run pytest`,

- **PRD ID**: PRD-0095
- **Title**: All dependency/build/test/run commands go through `uv` (`uv sync`, `uv add`, `uv run python`, `uv run pytest`,
- **Category**: Submission Checklist Item
- **Requirement source**: docs/06_submission_checklist.md
- **Implementation chunk**: 10/11
- **Status**: Done

## Requirement statement

The submission MUST satisfy: All dependency/build/test/run commands go through `uv` (`uv sync`, `uv add`, `uv run python`, `uv run pytest`, `uv lock`) — no `pip`, no `requirements.txt`.

## Rationale

Required by the submission checklist merged from both governing PDFs.

## Acceptance criteria

The checklist box is checked and is independently verifiable by review or script.

## Test / validation method

Manual review or automated script, per checklist section
