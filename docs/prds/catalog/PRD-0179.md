# PRD-0179 — --config <path> overrides the default config file

- **PRD ID**: PRD-0179
- **Title**: --config <path> overrides the default config file
- **Category**: CLI Behavior
- **Requirement source**: docs/prds/PRD-004-controller-and-orchestration.md + SG-C03
- **Implementation chunk**: 8
- **Status**: Not Started

## Requirement statement

The CLI MUST: --config <path> overrides the default config file.

## Rationale

Required for gradable CLI evidence (HW-F26) and SG-C03's SDK-only CLI rule.

## Acceptance criteria

A CLI smoke test or unit test verifies: --config <path> overrides the default config file.

## Test / validation method

CLI smoke test in tests/integration/
