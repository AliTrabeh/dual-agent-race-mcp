# TODO — Task Tracking

> Mandatory per Guidelines PDF SG-D02. Mirrors `docs/04_implementation_chunks.md` chunks 0–11. Status values: `not-started` / `in-progress` / `done`. Update this file as work proceeds — do not let it go stale (SG-D05).

## Phase 0 — Planning (this session)

| Task | Phase | Status | Definition of Done |
|------|-------|--------|----------------------|
| Read & extract requirements from both PDFs | 0 | done | `docs/00_source_analysis.md` complete, traceable |
| Build requirements traceability matrix | 0 | done | `docs/01_requirements_matrix.md` complete |
| Write master PRD + PLAN + this TODO | 1 | done | `docs/PRD.md`, `docs/PLAN.md`, `docs/TODO.md` exist |
| Write per-mechanism PRD for Q-Learning | 1 | done | `docs/PRD_q_learning.md` exists, covers SG-D03 fields |
| Write 7 chunk-level PRDs | 1 | done | `docs/prds/PRD-001..007-*.md` exist, ≥500 words each |
| Write architecture/testing/checklist/risk docs | 1 | done | `docs/03_architecture.md`, `05_testing_strategy.md`, `06_submission_checklist.md`, `07_risks_and_open_questions.md` exist |
| Write chunk implementation plan | 2 | done | `docs/04_implementation_chunks.md` complete for chunks 0-11 |
| Create project skeleton (uv + SDK architecture) | 3 | done | Directory tree from `01_requirements_matrix.md` exists, imports succeed |
| Write root README | 4 | done | README has all sections listed in PRD §SG-D01 |
| Minimal import-safe placeholder code + tests | 5 | done | 38/38 tests pass, 97.84% coverage, 0 ruff warnings (validated with `python -m pytest`/`ruff`; re-validate with `uv run` once `uv` is installed) |

## Phase 0b — Session constraints: line-limit gate + PRD catalog (this session, before Chunk 3)

| Task | Status | Definition of Done |
|------|--------|----------------------|
| Document the 150-physical-line Python file cap | done | `docs/PLAN.md` ADR-006, `docs/00_source_analysis.md` §9a (PROJ-R01), README contribution section |
| Add automated line-limit validation | done | `tests/test_line_limits.py` — passes (largest file: 87 lines) |
| Create PRD catalog (≥510 PRDs) | done | `docs/prds/catalog/PRD-0001..0522.md` (522 files), generated by `tools/generate_prd_catalog.py` from `tools/prd_catalog_data.json` |
| Create/update `docs/prds/PRD_INDEX.md` | done | Index lists all 522 catalog PRDs grouped by 21 categories |
| Update `docs/04_implementation_chunks.md` for the line-limit rule | done | Blanket note added + every chunk's Done criteria references `tests/test_line_limits.py` |
| Update `docs/06_submission_checklist.md` | done | New "§F. Project-specific session constraints" section added |
| Update `docs/01_requirements_matrix.md` | done | `PROJ-R01`/`PROJ-R02` rows added |

## Phase 1+ — Implementation chunks (not started until Phase 0 docs approved by user)

| Chunk | Title | Status | DoD reference |
|-------|-------|--------|-----------------|
| 0 | Project init & repo structure | done | `docs/04_implementation_chunks.md` §Chunk 0 |
| 1 | Requirements extraction & validation checklist | done (this session) | this file + matrix |
| 2 | Core config system + Gatekeeper + versioning | done | §Chunk 2 — `shared/config.py`, `shared/version.py`, `shared/gatekeeper.py` implemented + tested (15/15 tests, 97.84% cov, 0 ruff warnings) |
| 3 | MCP server/client layer | done | §Chunk 3 — `services/mcp/{auth,message_store,server_base,server_a,server_b,client}.py` implemented + tested (66/66 tests, 98.79% cov, 0 ruff warnings, all files ≤66 lines) |
| 4 | Agent abstraction layer | done | §Chunk 4 — `services/agents/{models,llm_client,base_agent,cop_agent,thief_agent,strategies/{base,heuristic_strategy}}.py` implemented + tested (112/112 tests, 99.20% cov, 0 ruff warnings, all files ≤88 lines) |
| 5 | Dual-agent race mechanism | done | §Chunk 5 — `services/race/{exceptions,models,race_state,race_engine,scoring}.py` implemented + tested (147/147 tests, 99.39% cov, 0 ruff warnings, all files ≤90 lines). Q-Learning stretch goal not attempted this pass. |
| 6 | Controller / orchestrator / game loop | done | §Chunk 6 — `sdk/{orchestrator,wiring}.py` + rewritten `sdk/sdk.py` implemented + tested (173/173 tests, 100% cov, 0 ruff warnings, all files ≤111 lines). Real end-to-end run verified manually (6/6 sub-games, totals match proven bound). Technical Loss containment implemented; full rerun-to-6 bookkeeping deferred to Chunk 7. |
| 7 | Logging, JSON protocol, run history, email | done | §Chunk 7 — `services/reporting/{schemas,bonus_report,technical_loss,run_logger,mailer}.py` implemented + tested (203/203 tests, 100% cov, 0 ruff warnings, all files ≤77 lines). Known limitation: `resolve_technical_losses` not yet wired into a single live match — see `docs/07_risks_and_open_questions.md`. |
| 8 | CLI interface | done | §Chunk 8 — `main.py` rewritten (argparse: --config/--dry-run/--log-level/--output-dir/--version), zero business logic, exit codes 0/1. Also closed a Chunk 6 gap: added the "full per-turn trace logging" step that chunk plan §6 had called for but wasn't implemented. 209/209 tests, 100% cov, 0 ruff warnings, main.py 89 lines. |
| 9 | Tests (unit + integration, coverage gate) | done | §Chunk 9 — added `tests/integration/test_staged_sanity_checks.py` (HW-F12's 6-grid-size matrix + Stage-1 message-pipeline-lossless check). 217/217 tests, 100% cov, 0 ruff warnings, all line-limit checks pass. No coverage/lint gaps existed to close (sustained 100% since Chunk 6). |
| 10 | Documentation finalization & submission packaging | done (with honest gaps) | §Chunk 10 — README rewritten with orchestration-challenge discussion, CLI run evidence, deployment guide (§8); `docs/06_submission_checklist.md` updated honestly with remaining gaps in §G (cloud deploy, OAuth, bonus round are all external/user-action items, not silently dropped). Real cloud deployment NOT performed (no cloud account available autonomously) — documented as an actionable guide instead. |
| 11 | Final validation against both PDFs | done | §Chunk 11 — re-audited `docs/01_requirements_matrix.md` row by row (fixed 9 stale ⬜ rows that were actually done: HW-F03, SG-C01/C02/C08/C14, SG-P02, SG-U04); performed the ISO/IEC 25010 self-check (`docs/06_submission_checklist.md` §H); final full gate re-run: 217/217 tests, 100% coverage, 0 ruff warnings, all line-limit checks pass. PRD catalog: 255/522 Done, 240 Not Started (mostly future-chunk granular items: cloud deploy specifics, bonus-round play, optional Q-Learning/GUI), 17 In Progress. Honest remaining gaps (cloud deploy, Gmail OAuth, bonus round, git branches/PRs/tags) all explicitly documented, none silently dropped. |

| 12 | LLM-driven decision strategy | done | §Chunk 12 — `services/agents/strategies/llm_strategy.py` (`LLMDecisionStrategy`): asks the LLM to choose the next action directly (not just compose/interpret messages), strictly parses UP/DOWN/LEFT/RIGHT/PLACE_BARRIER, falls back to `HeuristicStrategy` on any failure/ambiguity/illegal choice. Triggered by user feedback that fixed-step movement wasn't "thinking" about direction. Superseded as the *default* strategy by Chunk 13's minimax (still available via `decision_strategy: "llm"` in config). |
| 13 | Minimax + alpha-beta competitive decision strategy | done | §Chunk 13 — new `services/agents/strategies/minimax/` subpackage: depth-limited minimax with alpha-beta pruning, move ordering, and a transposition table, built on shared BFS/mobility/reachable-area utilities and role-aware `evaluate_for_cop`/`evaluate_for_thief` functions; heuristic one-ply fallback when search is unavailable. Now the default `decision_strategy` (config-driven, never hard-coded). Added `tools/simulate_strategies.py` for matchup benchmarking (Cop win rate, capture turn, barriers used, illegal actions) — confirms minimax Cop reliably beats Heuristic/Random Thief and a Heuristic/Random Cop never catches a Minimax Thief. 320/320 tests, 100% coverage, 0 ruff warnings, all files ≤150 physical lines. Honest finding: under the engine's real (HW-PDF-correct) one-way barrier rule, barriers never restrict the Thief, so a well-reasoned Cop correctly avoids them almost entirely — this is faithful behavior, not a bug. |

## Explicitly external / not trackable as code tasks

- Running the actual inter-group bonus match (HW-F27) — requires a second real student group; ≤1 week of assignment publication.
- Supplying real LLM provider credentials and Gmail OAuth credentials — user action, tracked in `docs/07_risks_and_open_questions.md`.
