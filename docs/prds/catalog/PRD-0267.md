# PRD-0267 — The Gmail dispatch call goes through the same Gatekeeper as LLM calls

- **PRD ID**: PRD-0267
- **Title**: The Gmail dispatch call goes through the same Gatekeeper as LLM calls
- **Category**: Architecture Decision
- **Requirement source**: docs/PLAN.md / docs/03_architecture.md
- **Implementation chunk**: varies
- **Status**: Not Started

## Requirement statement

The architecture MUST reflect: The Gmail dispatch call goes through the same Gatekeeper as LLM calls.

## Rationale

Recorded as an explicit ADR/architectural property so it is auditable rather than implicit.

## Acceptance criteria

Code review confirms the implementation matches: The Gmail dispatch call goes through the same Gatekeeper as LLM calls.

## Test / validation method

Code review against docs/PLAN.md and docs/03_architecture.md
