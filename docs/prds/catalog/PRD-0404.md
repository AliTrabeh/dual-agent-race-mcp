# PRD-0404 — ApiGatekeeper: each named service (default/llm/gmail) enforces its own configured limit

- **PRD ID**: PRD-0404
- **Title**: ApiGatekeeper: each named service (default/llm/gmail) enforces its own configured limit
- **Category**: Test Case
- **Requirement source**: docs/05_testing_strategy.md
- **Implementation chunk**: varies
- **Status**: In Progress

## Requirement statement

A dedicated test MUST verify, for ApiGatekeeper: each named service (default/llm/gmail) enforces its own configured limit.

## Rationale

SG-T01/T02 require TDD with a test per behavior, covering happy and edge/error paths.

## Acceptance criteria

The test passes deterministically with no dependency on a live external service (SG-T03).

## Test / validation method

pytest unit or integration test
