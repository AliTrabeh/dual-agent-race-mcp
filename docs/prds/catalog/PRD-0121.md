# PRD-0121 — config/rate_limits.json: services.llm.retry_after_seconds

- **PRD ID**: PRD-0121
- **Title**: config/rate_limits.json: services.llm.retry_after_seconds
- **Category**: Configuration Parameter
- **Requirement source**: config/rate_limits.json + SG-C06
- **Implementation chunk**: 2
- **Status**: Done

## Requirement statement

The `services.llm.retry_after_seconds` rate-limit parameter (current default `15`) MUST be centralized in JSON.

## Rationale

SG-C06 requires rate limits centralized in JSON, never hard-coded per call site.

## Acceptance criteria

`shared/gatekeeper.py` reads `services.llm.retry_after_seconds` from RateLimitConfig, never a literal.

## Test / validation method

Unit test in tests/unit/test_shared/test_gatekeeper.py
