# PRD-0447 — Chunk 2 (Core config system) — Steps

- **PRD ID**: PRD-0447
- **Title**: Chunk 2 (Core config system) — Steps
- **Category**: Chunk Specification Item
- **Requirement source**: docs/04_implementation_chunks.md
- **Implementation chunk**: 2
- **Status**: In Progress

## Requirement statement

Chunk 2's Steps MUST be: 1) define `constants.py` enums/defaults; 2) implement `ConfigLoader` reading + validating `config/setup.json` against required keys (HW-F25); 3) implement `ApiGatekeeper` per the literal interface in 

## Rationale

Each chunk's spec attribute is tracked individually so partial chunk progress stays auditable.

## Acceptance criteria

The implementation/process for Chunk 2 matches its documented Steps.

## Test / validation method

Manual review against docs/04_implementation_chunks.md at chunk completion
