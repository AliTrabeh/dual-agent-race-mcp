# PRD-0401 — ApiGatekeeper: a queue at max depth raises backpressure instead of growing unbounded

- **PRD ID**: PRD-0401
- **Title**: ApiGatekeeper: a queue at max depth raises backpressure instead of growing unbounded
- **Category**: Test Case
- **Requirement source**: docs/05_testing_strategy.md
- **Implementation chunk**: varies
- **Status**: Done

## Requirement statement

A dedicated test MUST verify, for ApiGatekeeper: a queue at max depth raises backpressure instead of growing unbounded.

## Rationale

SG-T01/T02 require TDD with a test per behavior, covering happy and edge/error paths.

## Acceptance criteria

The test passes deterministically with no dependency on a live external service (SG-T03).

## Test / validation method

pytest unit or integration test
