# PRD-0068 — `config.json`/`config.yaml` centralizes all game parameters — zero hard-coded game constants

- **PRD ID**: PRD-0068
- **Title**: `config.json`/`config.yaml` centralizes all game parameters — zero hard-coded game constants
- **Category**: Submission Checklist Item
- **Requirement source**: docs/06_submission_checklist.md
- **Implementation chunk**: 10/11
- **Status**: Not Started

## Requirement statement

The submission MUST satisfy: `config.json`/`config.yaml` centralizes all game parameters — zero hard-coded game constants.

## Rationale

Required by the submission checklist merged from both governing PDFs.

## Acceptance criteria

The checklist box is checked and is independently verifiable by review or script.

## Test / validation method

Manual review or automated script, per checklist section
