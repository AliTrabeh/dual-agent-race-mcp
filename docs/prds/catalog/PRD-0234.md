# PRD-0234 — .env must never be committed (checked via .gitignore + a secrets scan)

- **PRD ID**: PRD-0234
- **Title**: .env must never be committed (checked via .gitignore + a secrets scan)
- **Category**: Validation Rule
- **Requirement source**: docs/00_source_analysis.md + this session's constraints
- **Implementation chunk**: varies
- **Status**: Done

## Requirement statement

The project MUST enforce: .env must never be committed (checked via .gitignore + a secrets scan).

## Rationale

Required by the HW PDF, the Guidelines PDF, or this session's explicit new constraints.

## Acceptance criteria

An automated check (test, lint rule, or script) fails the build if this is violated.

## Test / validation method

Automated test, ruff rule, or validation script
