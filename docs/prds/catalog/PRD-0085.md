# PRD-0085 — `ApiGatekeeper` intercepts all outbound API calls (LLM + Gmail); rate limits centralized in `config/rate_limit

- **PRD ID**: PRD-0085
- **Title**: `ApiGatekeeper` intercepts all outbound API calls (LLM + Gmail); rate limits centralized in `config/rate_limit
- **Category**: Submission Checklist Item
- **Requirement source**: docs/06_submission_checklist.md
- **Implementation chunk**: 10/11
- **Status**: Not Started

## Requirement statement

The submission MUST satisfy: `ApiGatekeeper` intercepts all outbound API calls (LLM + Gmail); rate limits centralized in `config/rate_limits.json`.

## Rationale

Required by the submission checklist merged from both governing PDFs.

## Acceptance criteria

The checklist box is checked and is independently verifiable by review or script.

## Test / validation method

Manual review or automated script, per checklist section
