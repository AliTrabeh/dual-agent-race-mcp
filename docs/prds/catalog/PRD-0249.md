# PRD-0249 — LLM lives in the orchestrator (MCP client), never inside an MCP server

- **PRD ID**: PRD-0249
- **Title**: LLM lives in the orchestrator (MCP client), never inside an MCP server
- **Category**: Architecture Decision
- **Requirement source**: docs/PLAN.md / docs/03_architecture.md
- **Implementation chunk**: varies
- **Status**: Done

## Requirement statement

The architecture MUST reflect: LLM lives in the orchestrator (MCP client), never inside an MCP server.

## Rationale

Recorded as an explicit ADR/architectural property so it is auditable rather than implicit.

## Acceptance criteria

Code review confirms the implementation matches: LLM lives in the orchestrator (MCP client), never inside an MCP server.

## Test / validation method

Code review against docs/PLAN.md and docs/03_architecture.md
