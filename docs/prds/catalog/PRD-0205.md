# PRD-0205 — SchemaValidationError: raised when a generated report JSON fails schema validation

- **PRD ID**: PRD-0205
- **Title**: SchemaValidationError: raised when a generated report JSON fails schema validation
- **Category**: Error Handling Case
- **Requirement source**: docs/05_testing_strategy.md
- **Implementation chunk**: varies
- **Status**: Not Started

## Requirement statement

The system MUST define and raise `SchemaValidationError` when: raised when a generated report JSON fails schema validation.

## Rationale

SG-T05 requires explicit, documented, tested edge-case/error handling with graceful degradation.

## Acceptance criteria

A dedicated unit test triggers this condition and asserts the `SchemaValidationError` path is taken.

## Test / validation method

Unit test asserting the exception/handling path
