# PRD-0103 — config/setup.json: scoring.cop_win

- **PRD ID**: PRD-0103
- **Title**: config/setup.json: scoring.cop_win
- **Category**: Configuration Parameter
- **Requirement source**: config/setup.json + HW-F25
- **Implementation chunk**: 2
- **Status**: Not Started

## Requirement statement

The `scoring.cop_win` parameter (current default `20`) MUST be sourced from config, never hard-coded.

## Rationale

HW-F25 forbids hard-coded game parameters; SG-C08 forbids magic values in source.

## Acceptance criteria

`shared/config.py` exposes `scoring.cop_win`; all consumers read it via config, not a literal.

## Test / validation method

Unit test in tests/unit/test_shared/test_config.py
