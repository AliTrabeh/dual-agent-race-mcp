# PRD-0252 — Dec-POMDP element S maps to RaceState (grid, positions, barriers, move count)

- **PRD ID**: PRD-0252
- **Title**: Dec-POMDP element S maps to RaceState (grid, positions, barriers, move count)
- **Category**: Architecture Decision
- **Requirement source**: docs/PLAN.md / docs/03_architecture.md
- **Implementation chunk**: varies
- **Status**: Not Started

## Requirement statement

The architecture MUST reflect: Dec-POMDP element S maps to RaceState (grid, positions, barriers, move count).

## Rationale

Recorded as an explicit ADR/architectural property so it is auditable rather than implicit.

## Acceptance criteria

Code review confirms the implementation matches: Dec-POMDP element S maps to RaceState (grid, positions, barriers, move count).

## Test / validation method

Code review against docs/PLAN.md and docs/03_architecture.md
