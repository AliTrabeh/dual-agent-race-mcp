# PRD-0124 — config/rate_limits.json: services.gmail.requests_per_hour

- **PRD ID**: PRD-0124
- **Title**: config/rate_limits.json: services.gmail.requests_per_hour
- **Category**: Configuration Parameter
- **Requirement source**: config/rate_limits.json + SG-C06
- **Implementation chunk**: 2
- **Status**: Done

## Requirement statement

The `services.gmail.requests_per_hour` rate-limit parameter (current default `50`) MUST be centralized in JSON.

## Rationale

SG-C06 requires rate limits centralized in JSON, never hard-coded per call site.

## Acceptance criteria

`shared/gatekeeper.py` reads `services.gmail.requests_per_hour` from RateLimitConfig, never a literal.

## Test / validation method

Unit test in tests/unit/test_shared/test_gatekeeper.py
