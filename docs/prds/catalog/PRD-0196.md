# PRD-0196 — RateLimitExceededError: raised when a service's per-minute rate limit is reached

- **PRD ID**: PRD-0196
- **Title**: RateLimitExceededError: raised when a service's per-minute rate limit is reached
- **Category**: Error Handling Case
- **Requirement source**: docs/05_testing_strategy.md
- **Implementation chunk**: varies
- **Status**: Done

## Requirement statement

The system MUST define and raise `RateLimitExceededError` when: raised when a service's per-minute rate limit is reached.

## Rationale

SG-T05 requires explicit, documented, tested edge-case/error handling with graceful degradation.

## Acceptance criteria

A dedicated unit test triggers this condition and asserts the `RateLimitExceededError` path is taken.

## Test / validation method

Unit test asserting the exception/handling path
