# PRD-0106 — config/setup.json: scoring.thief_loss

- **PRD ID**: PRD-0106
- **Title**: config/setup.json: scoring.thief_loss
- **Category**: Configuration Parameter
- **Requirement source**: config/setup.json + HW-F25
- **Implementation chunk**: 2
- **Status**: Not Started

## Requirement statement

The `scoring.thief_loss` parameter (current default `5`) MUST be sourced from config, never hard-coded.

## Rationale

HW-F25 forbids hard-coded game parameters; SG-C08 forbids magic values in source.

## Acceptance criteria

`shared/config.py` exposes `scoring.thief_loss`; all consumers read it via config, not a literal.

## Test / validation method

Unit test in tests/unit/test_shared/test_config.py
