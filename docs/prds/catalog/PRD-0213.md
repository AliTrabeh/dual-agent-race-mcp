# PRD-0213 — The full match summary (6 sub-games + totals) is logged at INFO level on completion

- **PRD ID**: PRD-0213
- **Title**: The full match summary (6 sub-games + totals) is logged at INFO level on completion
- **Category**: Logging Requirement
- **Requirement source**: docs/03_architecture.md + SG-C09
- **Implementation chunk**: varies
- **Status**: Not Started

## Requirement statement

The system MUST ensure: The full match summary (6 sub-games + totals) is logged at INFO level on completion.

## Rationale

SG-T06 requires reportable run evidence; SG-C09 forbids secrets in logs; HW-F26 wants CLI evidence.

## Acceptance criteria

A unit/integration test using a log-capture fixture asserts: The full match summary (6 sub-games + totals) is logged at INFO level on completion.

## Test / validation method

Unit test with a log-capture fixture (e.g. pytest caplog)
