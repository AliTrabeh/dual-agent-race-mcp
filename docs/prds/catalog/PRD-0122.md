# PRD-0122 — config/rate_limits.json: services.llm.max_retries

- **PRD ID**: PRD-0122
- **Title**: config/rate_limits.json: services.llm.max_retries
- **Category**: Configuration Parameter
- **Requirement source**: config/rate_limits.json + SG-C06
- **Implementation chunk**: 2
- **Status**: Done

## Requirement statement

The `services.llm.max_retries` rate-limit parameter (current default `3`) MUST be centralized in JSON.

## Rationale

SG-C06 requires rate limits centralized in JSON, never hard-coded per call site.

## Acceptance criteria

`shared/gatekeeper.py` reads `services.llm.max_retries` from RateLimitConfig, never a literal.

## Test / validation method

Unit test in tests/unit/test_shared/test_gatekeeper.py
